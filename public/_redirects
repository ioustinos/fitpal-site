# API routes — must come BEFORE the SPA fallback, or /* eats them.
# Kept here (instead of in netlify.toml) because Netlify's resolved order
# puts _redirects rules first and we need /api/* to win.
/api/*  /.netlify/functions/:splat  200

# WEC-303: SEO crawler endpoints. Same rationale — must come BEFORE the
# SPA fallback or Lighthouse + Googlebot get the index.html HTML when
# they ask for plain text/XML. Status 200 (not 301) so the served
# content is from the actual file at that path, not the redirect target.
/robots.txt   /robots.txt   200
/sitemap.xml  /sitemap.xml  200

# ─── dev-host redirect REMOVED 2026-09-22, per Ioustinos ─────────────────
# Between 2026-09-17 and today, https://dev--fitpal-order.netlify.app/* was
# 302'd to production. A marketing campaign had gone out with the dev URL and
# real customers were ordering on the dev deploy — which is wired to the Viva
# DEMO merchant but to the SAME Supabase database, so sandbox payments wrote
# "paid" onto live orders and ~108 € was never collected.
#
# (The block used to cite "WEC-790". That was wrong — WEC-790 is the reseller
# zone time-slot half-ship. This guard was never ticketed at all.)
#
# Removed because it also made dev untestable for B2B storefronts (WEC-819,
# checkout form wiped on tab return on reseller stores).
#
# ⚠ The exposure that caused WEC-790 has NOT changed: dev still shares the
# production database and still points at the Viva demo merchant. If that
# campaign link is still circulating, orders can land here again. Re-add the
# two lines below to restore the guard:
#
#   https://dev--fitpal-order.netlify.app/order/*  /index.html  200
#   https://dev--fitpal-order.netlify.app/*  https://orders.fitpal.gr/:splat  302!

# SPA fallback — catch-all, last.
/*      /index.html                 200
